#
# -*- coding: utf-8 -*-
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import socket
from thrift import Thrift
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TCompactProtocol
from thrift.transport.TTransport import TTransportException
from comm.ttypes import *
from platform.machine.ttypes import *
from platform.machine import MachineManageBo

PYTHON_SUPPORT_DIR='/usr/local/soldier/route_agent'
if not (PYTHON_SUPPORT_DIR in sys.path):
  sys.path.append(PYTHON_SUPPORT_DIR)
from route_finder_python import *

class MachineManageBoStub:
  def __init__(self):
    self.__peerAddr = None

  def setPeerAddr(self, peerAddr):
    self.__peerAddr = peerAddr

  def __getServiceAddr(self, methodName='', routeKey=0):
    if self.__peerAddr != None and self.__peerAddr !='':
      return self.__peerAddr
    return route_finder.GetRouteIp(MachineManageBo.MachineManageBo_SERVICE_KEY, methodName, routeKey)

  def queryMachineList(self, routeKey, timeout, option,pageIndex,pageSize,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('queryMachineList', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.queryMachineList(platformArgs,option,pageIndex,pageSize,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'queryMachineList', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'queryMachineList', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def addMachine(self, routeKey, timeout, newMachine,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('addMachine', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.addMachine(platformArgs,newMachine,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'addMachine', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'addMachine', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def updateMachine(self, routeKey, timeout, updateMachine,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('updateMachine', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.updateMachine(platformArgs,updateMachine,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'updateMachine', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'updateMachine', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def deleteMachine(self, routeKey, timeout, hostName,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('deleteMachine', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.deleteMachine(platformArgs,hostName,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'deleteMachine', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'deleteMachine', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def updateMachineRelatedMonitor(self, routeKey, timeout, hostName,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('updateMachineRelatedMonitor', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.updateMachineRelatedMonitor(platformArgs,hostName,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'updateMachineRelatedMonitor', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'updateMachineRelatedMonitor', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def deleteMachineRelatedMonitor(self, routeKey, timeout, hostName,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('deleteMachineRelatedMonitor', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + MachineManageBo.MachineManageBo_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=MachineManageBo.Client(protocol)
    try:
      transport.open()
      result = client.deleteMachineRelatedMonitor(platformArgs,hostName,)
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'deleteMachineRelatedMonitor', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(MachineManageBo.MachineManageBo_SERVICE_KEY, 'deleteMachineRelatedMonitor', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

