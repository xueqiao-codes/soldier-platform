#
# -*- coding: utf-8 -*-
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import socket
from thrift import Thrift
from thrift.transport import TSocket
from thrift.transport import TTransport
from thrift.protocol import TCompactProtocol
from thrift.transport.TTransport import TTransportException
from comm.ttypes import *
from route_dao.ttypes import *
from route_dao import RouteDao

PYTHON_SUPPORT_DIR='/usr/local/soldier/route_agent'
if not (PYTHON_SUPPORT_DIR in sys.path):
  sys.path.append(PYTHON_SUPPORT_DIR)
from route_finder_python import *

class RouteDaoStub:
  def __init__(self):
    self.__peerAddr = None

  def setPeerAddr(self, peerAddr):
    self.__peerAddr = peerAddr

  def __getServiceAddr(self, methodName='', routeKey=0):
    if self.__peerAddr != None and self.__peerAddr !='':
      return self.__peerAddr
    return route_finder.GetRouteIp(RouteDao.RouteDao_SERVICE_KEY, methodName, routeKey)

  def addRoute(self, routeKey, timeout, route,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('addRoute', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.addRoute(platformArgs,route,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'addRoute', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'addRoute', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def updateRoute(self, routeKey, timeout, route,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('updateRoute', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.updateRoute(platformArgs,route,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'updateRoute', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'updateRoute', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def deleteRoute(self, routeKey, timeout, serviceKey,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('deleteRoute', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.deleteRoute(platformArgs,serviceKey,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'deleteRoute', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'deleteRoute', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def queryRouteInfoList(self, routeKey, timeout, pageIndex,pageSize,option,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('queryRouteInfoList', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.queryRouteInfoList(platformArgs,pageIndex,pageSize,option,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'queryRouteInfoList', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'queryRouteInfoList', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def syncRoute(self, routeKey, timeout, config,):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('syncRoute', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.syncRoute(platformArgs,config,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'syncRoute', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'syncRoute', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getLastRouteVersion(self, routeKey, timeout, ):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getLastRouteVersion', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.getLastRouteVersion(platformArgs,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getLastRouteVersion', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getLastRouteVersion', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getAllSimpleRouteInfo(self, routeKey, timeout, ):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getAllSimpleRouteInfo', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.getAllSimpleRouteInfo(platformArgs,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getAllSimpleRouteInfo', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getAllSimpleRouteInfo', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

  def getLastestRouteConfig(self, routeKey, timeout, ):
    platformArgs=PlatformArgs()
    frame = sys._getframe(1)
    code = frame.f_code
    platformArgs.sourceDesc = code.co_filename + '[' + code.co_name + ":" + str(code.co_firstlineno) + ']'
    platformArgs.sourceIp = socket.gethostbyname(socket.getfqdn())
    platformArgs.remoteAddress = self.__getServiceAddr('getLastestRouteConfig', routeKey)
    if platformArgs.remoteAddress == None or platformArgs.remoteAddress == '':
      raise Exception('No RouteIp is Found')
    platformArgs.remotePort = 10000 + RouteDao.RouteDao_SERVICE_KEY

    transport_socket = TSocket.TSocket(platformArgs.remoteAddress, platformArgs.remotePort)

    transport_socket.setTimeout(timeout)
    transport = TTransport.TFramedTransport(transport_socket)
    protocol = TCompactProtocol.TCompactProtocol(transport)
    client=RouteDao.Client(protocol)
    try:
      transport.open()
      result = client.getLastestRouteConfig(platformArgs,)
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getLastestRouteConfig', platformArgs.remoteAddress, 0)
      return result
    except TTransportException,t:
      route_finder.UpdateCallInfo(RouteDao.RouteDao_SERVICE_KEY, 'getLastestRouteConfig', platformArgs.remoteAddress, 1)
      raise t
    finally:
      transport.close()

